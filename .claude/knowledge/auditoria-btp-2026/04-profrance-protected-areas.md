# ProFrance — Protected Areas

## Estado: BLOQUEADO

Não há código, arquivos de código, áreas legadas protegidas, `protected files` ou restrições fixas de repositório neste Project. — `UNKNOWN`

Sem base de código visível, não existe nada a proteger nem legado a preservar. Marcar áreas protegidas inventadas seria falso, então este documento fica intencionalmente vazio de conteúdo técnico.

## Única coisa que se aproxima de "restrição fixa"

São **restrições de estilo** definidas pela usuária, não restrições de código:

- Nunca usar travessão (—) em texto gerado. — `USER-DEFINED RULE`
- Nunca usar paleta amarela/bege/quente em deliverables visuais pessoais. — `USER-DEFINED RULE`
- Na skill `cinematic-cartoon`: nada inclinado, nunca usar `rotate()`, hover sempre vertical limpo (`translateY` + sombra). — `CONFIRMED` (regra da skill)

## O que falta para desbloquear

Se e quando houver repositório, seriam registrados aqui, com identificadores exatos em inglês:
- `protected files` (arquivos que não devem ser editados por agente) — `UNKNOWN`
- Áreas legadas a preservar — `UNKNOWN`
- Restrições fixas de deploy/branch — `UNKNOWN`
